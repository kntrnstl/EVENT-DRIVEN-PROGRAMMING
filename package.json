{
    "name": "full-stack",
    "version": "1.0.0",
    "scripts": {
        "start": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\""
    },
    "devDependencies": {
        "concurrently": "^9.0.0"
    },
    "dependencies": {
        "chart.js": "^4.5.1",
        "nodemailer": "^7.0.11",
        "normalize.css": "^8.0.1"
    }
}
